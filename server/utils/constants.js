// Definição das constantes
const idInstance = process.env.GREEN_INSTANCE_ID;
const apiTokenInstance = process.env.GREEN_TOKEN;
const apiUrlWhats = "https://api.greenapi.com";
const minutes = 1440;

// Exporta as constantes
module.exports = {
    idInstance,
    apiTokenInstance,
    apiUrl,/*9
    minutes
};
